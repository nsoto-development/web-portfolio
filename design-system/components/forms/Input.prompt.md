Single-line text field for contact forms, search, subscribe boxes.

```jsx
<Input label="Email" placeholder="you@domain.com" />
<Input label="Name" error="Required" />
```

Cyan glow ring on focus (not just border color) since the UI is dark and color-only focus states are hard to see.
