Segmented control for switching project-category filters ("All / Web / Mobile / OSS").

```jsx
<Tabs items={[{label:"All", value:"all"},{label:"Web", value:"web"}]} value={tab} onChange={setTab} />
```
