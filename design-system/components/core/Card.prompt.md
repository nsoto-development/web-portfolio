Base surface container for grouping content — project tiles, stat blocks, form panels.

```jsx
<Card>Static content panel</Card>
<Card interactive onClick={() => {}}>Project tile — lifts + glows on hover</Card>
```

Set `interactive` for clickable cards (adds hover lift, cyan border, glow shadow). Static cards stay flat with just a hairline border.
