Interactive chip for tech-stack labels ("React", "Go", "Postgres") or filter toggles — clickable, optionally removable, optionally selected.

```jsx
<Tag>TypeScript</Tag>
<Tag selected onClick={() => {}}>All projects</Tag>
<Tag onRemove={() => {}}>Remove me</Tag>
```
